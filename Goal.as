package{	import flash.display.MovieClip;	import flash.events.Event;		public class Goal extends MovieClip	{		public function Goal()		{			init();		}		function init():void		{			addEventListener(Event.ADDED_TO_STAGE, onAddedToStage);		}		private function onAddedToStage(event:Event):void		{			addEventListener(Event.ENTER_FRAME, onEnterFrame);			addEventListener(Event.REMOVED_FROM_STAGE, onRemovedFromStage);			removeEventListener(Event.ADDED_TO_STAGE, onAddedToStage);		}		private function onRemovedFromStage(event:Event):void		{			removeEventListener(Event.ENTER_FRAME, onEnterFrame);			removeEventListener(Event.REMOVED_FROM_STAGE, onRemovedFromStage)		}		function onEnterFrame(event:Event):void		{			if (collisionArea.hitTestObject(Main.currentObject.player.collisionArea)) {				Main.currentObject.scene++;				Shared.passSceneReset();			}			else if (glowArea.hitTestObject(Main.currentObject.player.collisionArea)) {				Collision.lightUpThatGoalKid(MovieClip(Main.currentObject.player), this);			}		}	}}